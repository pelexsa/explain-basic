namespace
충돌 방지기법
using namespace std; // std 네임스페이스를 사용한다고 명시하여
std안에 있는 명령어는 std::cout이 아닌, cout 으로 처리 가능

sizeof
포인터가 가르키고 있는 주소에 데이터 크기를 byte로 표현

자료형 - int a = 4; sizeof(4); sizeof(int); 자료형의 데이터 크기
배열 - int a[] = { 5, 4, 3 }; sizeof(a); 배열의 크기 
sizeof(자료형)을 입력하는 경우 자료형의 크기 출력
